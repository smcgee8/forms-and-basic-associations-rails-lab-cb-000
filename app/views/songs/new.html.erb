<h1>Create a Song</h1>

<%= form_for @song do |f| %>
  <%= f.label :title %>
  <%= f.text_field :title %>
  <br />

  <%= f.label :artist_name, :artist %>
  

  <%= f.label :genre %>
  <%= f.collection_select :genre, Genre.all, :id, :name %>



  <%= f.submit %>
<% end %>
